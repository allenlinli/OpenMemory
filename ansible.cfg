# Ansible Configuration for OpenMemory
# This configuration enables automatic vault password handling

[defaults]
# Automatically use vault password file
vault_password_file = .vault_pass

# Optionally set inventory location
# inventory = ./inventory

# Set default remote user (if using Ansible for deployment)
# remote_user = deploy

# Disable host key checking for localhost operations
host_key_checking = False
